{
  "name": "brokierouter",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "fetch:endpoints": "node --env-file=.env src/fetch-endpoints.ts",
    "build": "node --env-file=.env src/build.ts",
    "benchmark": "node --env-file=.env src/benchmark.ts",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "valibot": "^1.3.1"
  }
}
