{
  "name": "brokierouter",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "fetch:endpoints": "node --env-file-if-exists=.env src/fetch-endpoints.ts",
    "benchmark": "node --env-file-if-exists=.env src/benchmark.ts",
    "build": "node --env-file-if-exists=.env src/build.ts",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "valibot": "^1.3.1"
  }
}
